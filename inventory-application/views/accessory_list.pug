extends layout

block content
  h1= title

  ul 
    each accessory in accessory_list 
        li 
            a(href=accessory.url) #{accessory.name} 
            | #{accessory.price} 
            br           
            | #{accessory.description}

    else 
        lis There are no accessories